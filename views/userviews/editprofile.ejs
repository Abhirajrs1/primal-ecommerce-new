<%- include('header') %>
<%- include('nav') %>
<%- include('cdmlinks') %>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card mb-4">
                <div class="card-body user-details-section" id="userDetailsSection">
                    <!-- Existing content -->
                    <!-- ... -->
                    <h4>Edit Profile</h4>
                    <form action="/editprofile?id=<%=user.useremail%>" method="post">
                        <div class="mb-3">
                            <label for="fullName" class="form-label">Full Name</label>
                            <input type="text" class="form-control" id="fullName" name="fullName" value="<%= locals.user.username %>" required>
                        </div>
                        <div class="mb-3">
                            <label for="mobileNumber" class="form-label">Mobile Number</label>
                            <input type="tel" class="form-control" id="mobileNumber" name="mobileNumber" value="<%= locals.user.usercontact %>" required>
                        </div>
                        <button type="submit" class="btn btn-primary">Save Changes</button>
                    </form>
                    <!-- ... -->
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('footer') %>
